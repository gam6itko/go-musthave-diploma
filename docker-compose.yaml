version: '3.1'

services:
  postgres:
    container_name: yp-musthave-diploma-postgres
    image: postgres:13
    environment:
      POSTGRES_DB: yp_diploma
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data

volumes:
  pg-data: ~
